# The Odds API key (https://the-odds-api.com/, free tier: 500 credits/month).
# Locally: put it here and `export $(cat .env)`; on GitHub: add it as the ODDS_API_KEY repository secret.
ODDS_API_KEY=your_api_key_here

# Optional override; the pipeline infers the season from the date.
# SEASON=2026
